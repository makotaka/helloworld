name: helloworld
on:
  push:
    branches: [ on_necci ]
  pull_request:
    branches: [ on_necci ]
  workflow_dispatch:
jobs:
  helloworld:
    runs-on: [self-hosted, necci2]
    steps:
      - run: |
          uname -a
          echo Hello, world!
  githubhosted:
    runs-on: ubuntu-18.04
    steps:
      - run: |
          uname -a
          echo Hello, world!
    

