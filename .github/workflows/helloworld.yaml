name: helloworld
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  helloworld:
    runs-on: [self-hosted, necci]
    steps:
      - run: |
          uname -a
          echo Hello, world!

